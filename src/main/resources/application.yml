spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/b4rrhh
    username: b4rrhh
    password: b4rrhh
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

management:
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
  endpoints:
    web:
      exposure:
        include: health
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

logging:
  level:
    org.flywaydb: info

app:
  jwt:
    secret: "b4rrhh-dev-secret-key-minimum-256-bits-for-hmac-sha256-algorithm"

payroll:
  launch:
    execution:
      mode: ELIGIBLE_REAL
      collapse-segment-rows: true

minio:
  endpoint: http://localhost:9000
  access-key: b4rrhh
  secret-key: b4rrhh123
  bucket-name: b4rrhh-employee-photos
  presigned-url-expiry-minutes: 10